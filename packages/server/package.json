{
  "name": "@beadwork/server",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "build": "bun build src/index.ts --target bun --outdir ./dist",
    "start": "bun run dist/index.js",
    "test": "bun test"
  },
  "dependencies": {
    "@mariozechner/pi-coding-agent": "^0.27.2",
    "hono": "^4.6.16",
    "zod": "^4.2.1"
  },
  "devDependencies": {
    "@types/bun": "^1.1.14"
  }
}
